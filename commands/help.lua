local json = require("json")

command.Register("help", "All the help one could ask for!", function(msg, args)
--	local buyData = httpR.Get("https://api.coinbase.com/v2/prices/XRP-GBP/buy")
--	buyData = json.decode(buyData)
--	buyData = buyData['data']
--	
--	p(buyData)
end)